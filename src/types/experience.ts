import { IconType } from "react-icons";

export interface IProfessionalExperience {
  icon: IconType;
  company: string;
  duration: string;
  short: string;
  description: string;
  designation: string;
  slug: string;
  url: string;
}
